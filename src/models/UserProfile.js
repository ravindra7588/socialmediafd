class UserProfile {
    constructor(about, createdOn,gender,languages,profilePicUrl) {
        this.about = about;
      this.createdOn = createdOn;
      this.gender = gender;
      this.languages = languages;
      this.profilePicUrl=profilePicUrl;
      
    }
  }
  
  export default UserProfile;